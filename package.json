{
  "private": true,
  "scripts": {
    "start": "npx eleventy --serve --quiet",
    "build": "npx eleventy --quiet",
    "test": "stylelint 'src/styles/**/*.css'"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.9.0",
    "full-icu": "^1.3.0",
    "pretty-data": "^0.40.0",
    "stylelint": "^11.0.0"
  }
}

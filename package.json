{
  "private": true,
  "scripts": {
    "start": "NODE_ICU_DATA=$(pwd)/node_modules/full-icu eleventy --serve --quiet",
    "build": "NODE_ICU_DATA=$(pwd)/node_modules/full-icu eleventy --quiet"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.9.0",
    "full-icu": "^1.3.0",
    "pretty-data": "^0.40.0"
  }
}

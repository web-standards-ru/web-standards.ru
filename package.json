{
  "private": true,
  "scripts": {
    "start": "NODE_ICU_DATA=\"$(pwd)/node_modules/full-icu\" npx eleventy --serve --quiet",
    "build": "NODE_ICU_DATA=\"$(pwd)/node_modules/full-icu\" npx eleventy --quiet",
    "test": "stylelint 'src/styles/**/*.css'"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.9.0",
    "full-icu": "^1.3.0",
    "pretty-data": "^0.40.0",
    "stylelint": "^11.0.0"
  },
  "browserslist": [
    "last 2 version",
    "not dead",
    "not ie <= 11"
  ]
}
